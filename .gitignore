.DS_Store
*.log
node_modules/
# real video dropped in for looking at by hand, never part of the repo
*.mkv
*.mp4
test/site/The Legend of Vox Machina*/

# working data during development, never in the repo
.dev/

# what Dima drops in by hand for me to place: never part of the repo
user_input/

# the project's own notes: kept in their own repository in .internal/, never public
.internal/
CLAUDE.md
# the page for recording the seal, a tool, not the product
web/seal.html

# only the fonts live in web/assets: brand marks and screenshots are not part of the code
web/assets/*.png
web/assets/*.svg
